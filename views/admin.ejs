<%- include("templates/header") %>

<h1 class="text-center mt-5">Admin Dashboard</h1>

<table class="table table-striped text-center mt-3 w-75 mx-auto">
  <thead>
    <tr>
      <th>Username</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.user_type %></td>
        <td>
<td>
  <div class="d-flex justify-content-center gap-2">
    <form method="POST" action="/promote">
      <input type="hidden" name="username" value="<%= user.username %>">
      <button class="btn btn-danger btn-sm rounded-pill px-3">Promote to Admin</button>
    </form>

    <form method="POST" action="/demote">
      <input type="hidden" name="username" value="<%= user.username %>">
      <button class="btn btn-success btn-sm rounded-pill px-3">Demote to User</button>
    </form>
  </div>
</td>

        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include("templates/footer") %>
