<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>COMP2537 Assignment 2</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/headers.css" rel="stylesheet">
</head>
<body>
  <header class="p-3 text-bg-dark">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
          <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
          <span class="fs-4">COMP2537</span>
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <% if (session.authenticated) { %>
            <% if (session.user_type === 'admin') { %>
              <li><a href="/admin" class="nav-link px-2 text-white">Admin</a></li>
            <% } %>
            <li><a href="/members" class="nav-link px-2 text-white">Members</a></li>
          <% } else { %>
            <li><a href="/" class="nav-link px-2 text-white">Home</a></li>
          <% } %>
        </ul>

        <div class="text-end">
          <% if (session.authenticated) { %>
            <a href="/logout" class="btn btn-outline-light me-2">Logout</a>
          <% } else { %>
            <a href="/login" class="btn btn-outline-light me-2">Login</a>
            <a href="/createUser" class="btn btn-warning">Sign-up</a>
          <% } %>
        </div>
      </div>
    </div>
  </header>
  <main class="container mt-4">
